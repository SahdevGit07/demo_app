import 'package:flutter/material.dart';

class ColorChangPressButton extends StatefulWidget {
  const ColorChangPressButton({super.key});

  @override
  State<ColorChangPressButton> createState() => _ColorChangPressButtonState();
}

class _ColorChangPressButtonState extends State<ColorChangPressButton> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
    );
  }
}
